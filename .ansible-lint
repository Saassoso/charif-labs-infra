---
# Tell the linter to bypass checking for these raw external modules in CI
mock_modules:
  - ansible.posix.synchronize
  - ansible.posix.sysctl
exclude_paths:
  - .github/
  - docker/
warn_list:
  - args
  - yaml
