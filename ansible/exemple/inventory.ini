<Your_VM_IP>

---
- name: Configure NTP on the VM
  hosts: ansible_host
  become: true
  tasks:
    - name: Install NTP
      ansible.builtin.apt:
        name: ntp
        state: latest

ansible-playbook -i hosts NTP_Installed.yaml