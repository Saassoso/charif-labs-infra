---
- name: Provision and Harden Linux Docker Host
  hosts: docker_hosts
  become: true
  vars_files:
    - ../vault.yml

  roles:
    - common
    - docker_engine
    - tailscale
    - linux_hardening
